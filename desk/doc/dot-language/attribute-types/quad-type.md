# quadType

Quadtree scheme

  * `"normal"`
  * `"fast"`
  * `"none"`



Using `"fast"` gives about a 2-4 times overall speedup compared with `"normal"`, though layout quality can suffer a little.

## Attributes

`quadType` is a valid type for:

  * [quadtree](../attributes/quadtree.md)
