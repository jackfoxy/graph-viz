# viewport

Clipping window on final drawing

type: [viewPort](../attribute-types/viewPort.md), default: `""`

`viewport` supersedes any [size](size.md) attribute. The width and height of the viewport specify precisely the final size of the output.

_Valid on:_

  * Graphs
