# packmode

How connected components should be packed

type: [packMode](../attribute-types/packMode.md), default: `node`

(cf [packMode](../attribute-types/packMode.md)). Note that defining `packmode` will automatically turn on packing as though one had set `pack=true`.

_Valid on:_

  * Graphs
